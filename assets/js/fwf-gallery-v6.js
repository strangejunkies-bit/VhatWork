(()=>{const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];const body=document.body;qa('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const value=btn.dataset.filter;qa('[data-filter]').forEach(b=>{b.classList.toggle('is-active',b===btn);b.setAttribute('aria-pressed',String(b===btn))});qa('[data-gallery-category]').forEach(card=>{card.hidden=value!=='all'&&card.dataset.galleryCategory!==value})}));let overlay,img,items=[],index=0,lastFocus=null;function eligible(){return qa('[data-lightbox]').filter(el=>!el.hidden&&!el.closest('[hidden]'))}function ensure(){if(overlay)return;overlay=document.createElement('div');overlay.className='lightbox';overlay.setAttribute('role','dialog');overlay.setAttribute('aria-modal','true');overlay.setAttribute('aria-label','Project image viewer');overlay.innerHTML='<div class="lightbox-stage"><button class="lightbox-close" type="button" aria-label="Close image viewer">×</button><button class="lightbox-prev" type="button" aria-label="Previous project">←</button><img alt=""><button class="lightbox-next" type="button" aria-label="Next project">→</button></div>';body.appendChild(overlay);img=q('img',overlay);q('.lightbox-close',overlay).addEventListener('click',close);q('.lightbox-prev',overlay).addEventListener('click',()=>move(-1));q('.lightbox-next',overlay).addEventListener('click',()=>move(1));overlay.addEventListener('click',e=>{if(e.target===overlay)close()})}function source(el){const im=el.tagName==='IMG'?el:q('img',el);return{src:el.dataset.full||im?.currentSrc||im?.src||'',alt:im?.alt||'Project image'}}function render(){if(!items.length)return;index=(index+items.length)%items.length;const x=source(items[index]);img.src=x.src;img.alt=x.alt}function open(el){ensure();items=eligible();index=Math.max(0,items.indexOf(el));lastFocus=document.activeElement;render();overlay.classList.add('open');body.classList.add('is-locked');q('.lightbox-close',overlay).focus()}function close(){if(!overlay?.classList.contains('open'))return;overlay.classList.remove('open');body.classList.remove('is-locked');lastFocus?.focus?.()}function move(n){index+=n;render()}qa('[data-lightbox]').forEach(el=>{if(!['BUTTON','A'].includes(el.tagName)){el.tabIndex=0;el.setAttribute('role','button')}el.addEventListener('click',e=>{if(el.tagName==='A')e.preventDefault();open(el)});el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open(el)}})});document.addEventListener('keydown',e=>{if(!overlay?.classList.contains('open'))return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)});})();