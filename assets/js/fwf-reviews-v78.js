(()=>{
  const section=document.querySelector('[data-google-reviews]');if(!section)return;
  const endpoint=section.dataset.reviewsEndpoint,grid=section.querySelector('[data-google-review-grid]'),ratingEl=section.querySelector('[data-google-rating]'),starsEl=section.querySelector('[data-google-stars]'),countEl=section.querySelector('[data-google-review-count]'),reviewsLink=section.querySelector('[data-google-reviews-link]'),profileLink=section.querySelector('[data-google-profile-link]'),writeLink=section.querySelector('[data-google-write-review]');
  const fallback='https://maps.app.goo.gl/EJw92vgT3aAMV26S9?g_st=aw';let loaded=false;
  const starText=rating=>'★'.repeat(Math.max(0,Math.min(5,Math.round(Number(rating)||0))))+'☆'.repeat(Math.max(0,5-Math.round(Number(rating)||0)));
  const safeUrl=(value,fallbackUrl=fallback)=>{try{const u=new URL(value);return u.protocol==='https:'?u.href:fallbackUrl}catch{return fallbackUrl}};
  const make=(tag,cls,text)=>{const el=document.createElement(tag);if(cls)el.className=cls;if(text!=null)el.textContent=text;return el};
  const renderReview=review=>{const card=make('article','google-review-card'),author=make('div','google-review-author'),attr=review.author||{};if(attr.photoUri){const img=make('img','google-review-avatar');img.src=safeUrl(attr.photoUri,'');img.alt=`${attr.displayName||'Google reviewer'} profile photo`;img.loading='lazy';img.decoding='async';img.referrerPolicy='no-referrer';img.width=42;img.height=42;author.append(img)}const authorCopy=make('div','google-review-author-copy');if(attr.uri){const a=make('a',null,attr.displayName||'Google reviewer');a.href=safeUrl(attr.uri);a.target='_blank';a.rel='noopener';authorCopy.append(a)}else authorCopy.append(make('strong',null,attr.displayName||'Google reviewer'));if(review.relativeTime)authorCopy.append(make('span','google-review-time',review.relativeTime));author.append(authorCopy);card.append(author);const stars=make('div','google-review-stars',starText(review.rating));stars.setAttribute('aria-label',`${review.rating||0} out of 5 stars`);card.append(stars);if(review.text)card.append(make('p','google-review-text',review.text));const source=make('a','google-review-source','View on Google Maps →');source.href=safeUrl(review.googleMapsUri);source.target='_blank';source.rel='noopener';card.append(source);return card};
  const showFallback=()=>{grid.setAttribute('aria-busy','false');grid.replaceChildren();ratingEl.textContent='—';countEl.textContent='Google reviews';if(starsEl)starsEl.setAttribute('aria-hidden','true');const box=make('div','google-reviews-error');box.append(make('p',null,'Current reviews are available directly on Google Maps.'));const a=make('a',null,'Open Google Reviews →');a.href=fallback;a.target='_blank';a.rel='noopener';box.append(a);grid.append(box)};
  async function load(){if(loaded)return;loaded=true;const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),8000);try{const r=await fetch(endpoint,{headers:{Accept:'application/json'},cache:'no-store',credentials:'omit',signal:controller.signal});if(!r.ok)throw new Error('Reviews unavailable');const data=await r.json(),rating=Number(data.rating);if(Number.isFinite(rating)){ratingEl.textContent=rating.toFixed(1);if(starsEl){starsEl.textContent=starText(rating);starsEl.setAttribute('aria-hidden','false');starsEl.setAttribute('aria-label',`${rating.toFixed(1)} out of 5 stars`)}}if(Number.isFinite(Number(data.reviewCount)))countEl.textContent=`${Number(data.reviewCount).toLocaleString()} Google reviews`;const profile=safeUrl(data.reviewsUri||data.googleMapsUri);[reviewsLink,profileLink].forEach(a=>{if(a)a.href=profile});if(writeLink)writeLink.href=safeUrl(data.writeAReviewUri||data.googleMapsUri);grid.replaceChildren();const list=Array.isArray(data.reviews)?data.reviews:[];list.filter(r=>r&&r.text).slice(0,5).forEach(r=>grid.append(renderReview(r)));grid.setAttribute('aria-busy','false');if(!grid.children.length)showFallback()}catch{showFallback()}finally{clearTimeout(timeout)}}
  if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting)){io.disconnect();load()}},{rootMargin:'700px 0px'});io.observe(section)}else load();
})();